[package]
name = "smpp"
version = "0.1.0"
authors = ["Iain H"]
edition = "2018"

[dependencies]
bytes = "1.0"
num_enum = "0.5"
tokio = { version = "1.0", features = ["full"] }

[dev-dependencies]
argh = "0.1"

[[example]]
name = "send_sms"